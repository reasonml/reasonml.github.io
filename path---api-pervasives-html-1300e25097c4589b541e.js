webpackJsonp([221],{"./node_modules/json-loader/index.js!./.cache/json/api-pervasives-html.json":function(e,n){e.exports={data:{file:{relativePath:"api/Pervasives.html",childRawHtml:{content:'<div class="ocamldoc">\n<div class="navbar"><a class="pre" href="Parsing.html" title="Parsing">Previous</a>\n&#xA0;<a class="up" href="index.html" title="Index">Up</a>\n&#xA0;<a class="post" href="Printexc.html" title="Printexc">Next</a>\n</div>\n<h1>Module <a href="type_Pervasives.html">Pervasives</a></h1>\n\n<pre><span class="keyword">module</span> Pervasives: sig .. end</pre><div class="info module top">\nThe initially opened module.\n<p>\n\n   This module provides the basic operations over the built-in types\n   (numbers, booleans, byte sequences, strings, exceptions, references,\n   lists, arrays, input-output channels, ...).\n</p><p>\n\n   This module is automatically opened at the beginning of each compilation.\n   All components of this module can therefore be referred by their short\n   name, without prefixing them by <code class="code">Pervasives</code>.<br>\n</p></div>\n<hr width="100%">\n<br>\n<h6 id="6_Exceptions">Exceptions</h6><br>\n\n<pre><span class="keyword">let</span> raise: exn =&gt; &apos;a;\n</pre><div class="info ">\nRaise the given exception value<br>\n</div>\n\n<pre><span class="keyword">let</span> raise_notrace: exn =&gt; &apos;a;\n</pre><div class="info ">\nA faster version <code class="code">raise</code> which does not record the backtrace.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> invalid_arg: string =&gt; &apos;a;\n</pre><div class="info ">\nRaise exception <code class="code">Invalid_argument</code> with the given string.<br>\n</div>\n\n<pre><span class="keyword">let</span> failwith: string =&gt; &apos;a;\n</pre><div class="info ">\nRaise exception <code class="code">Failure</code> with the given string.<br>\n</div>\n\n<pre><span class="keyword">exception</span> Exit;\n</pre>\n<div class="info ">\nThe <code class="code">Exit</code> exception is not raised by any library function.  It is\n    provided for use in your programs.<br>\n</div>\n<br>\n<h6 id="6_Comparisons">Comparisons</h6><br>\n\n<pre><span class="keyword">let</span> (=): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\n<code class="code">e1 = e2</code> tests for structural equality of <code class="code">e1</code> and <code class="code">e2</code>.\n   Mutable structures (e.g. references and arrays) are equal\n   if and only if their current contents are structurally equal,\n   even if the two mutable objects are not the same physical object.\n   Equality between functional values raises <code class="code">Invalid_argument</code>.\n   Equality between cyclic data structures may not terminate.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&lt;&gt;): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\nNegation of <a href="Pervasives.html#VAL(=)"><code class="code">Pervasives.(=)</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&lt;): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\nSee <a href="Pervasives.html#VAL(&gt;=)"><code class="code">Pervasives.(&gt;=)</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&gt;): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\nSee <a href="Pervasives.html#VAL(&gt;=)"><code class="code">Pervasives.(&gt;=)</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&lt;=): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\nSee <a href="Pervasives.html#VAL(&gt;=)"><code class="code">Pervasives.(&gt;=)</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&gt;=): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\nStructural ordering functions. These functions coincide with\n   the usual orderings over integers, characters, strings, byte sequences\n   and floating-point numbers, and extend them to a\n   total ordering over all types.\n   The ordering is compatible with <code class="code">( = )</code>. As in the case\n   of <code class="code">( = )</code>, mutable structures are compared by contents.\n   Comparison between functional values raises <code class="code">Invalid_argument</code>.\n   Comparison between cyclic structures may not terminate.<br>\n</div>\n\n<pre><span class="keyword">let</span> compare: &apos;a =&gt; &apos;a =&gt; int;\n</pre><div class="info ">\n<code class="code">compare x y</code> returns <code class="code">0</code> if <code class="code">x</code> is equal to <code class="code">y</code>,\n   a negative integer if <code class="code">x</code> is less than <code class="code">y</code>, and a positive integer\n   if <code class="code">x</code> is greater than <code class="code">y</code>.  The ordering implemented by <code class="code">compare</code>\n   is compatible with the comparison predicates <code class="code">=</code>, <code class="code">&lt;</code> and <code class="code">&gt;</code>\n   defined above,  with one difference on the treatment of the float value\n   <a href="Pervasives.html#VALnan"><code class="code">Pervasives.nan</code></a>.  Namely, the comparison predicates treat <code class="code">nan</code>\n   as different from any other float value, including itself;\n   while <code class="code">compare</code> treats <code class="code">nan</code> as equal to itself and less than any\n   other float value.  This treatment of <code class="code">nan</code> ensures that <code class="code">compare</code>\n   defines a total ordering relation.\n<p>\n\n   <code class="code">compare</code> applied to functional values may raise <code class="code">Invalid_argument</code>.\n   <code class="code">compare</code> applied to cyclic structures may not terminate.\n</p><p>\n\n   The <code class="code">compare</code> function can be used as the comparison function\n   required by the <a href="Set.Make.html"><code class="code">Set.Make</code></a> and <a href="Map.Make.html"><code class="code">Map.Make</code></a> functors, as well as\n   the <a href="List.html#VALsort"><code class="code">List.sort</code></a> and <a href="Array.html#VALsort"><code class="code">Array.sort</code></a> functions.<br>\n</p></div>\n\n<pre><span class="keyword">let</span> min: &apos;a =&gt; &apos;a =&gt; &apos;a;\n</pre><div class="info ">\nReturn the smaller of the two arguments.\n    The result is unspecified if one of the arguments contains\n    the float value <code class="code">nan</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> max: &apos;a =&gt; &apos;a =&gt; &apos;a;\n</pre><div class="info ">\nReturn the greater of the two arguments.\n    The result is unspecified if one of the arguments contains\n    the float value <code class="code">nan</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (==): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\n<code class="code">e1 == e2</code> tests for physical equality of <code class="code">e1</code> and <code class="code">e2</code>.\n   On mutable types such as references, arrays, byte sequences, records with\n   mutable fields and objects with mutable instance variables,\n   <code class="code">e1 == e2</code> is true if and only if physical modification of <code class="code">e1</code>\n   also affects <code class="code">e2</code>.\n   On non-mutable types, the behavior of <code class="code">( == )</code> is\n   implementation-dependent; however, it is guaranteed that\n   <code class="code">e1 == e2</code> implies <code class="code">compare e1 e2 = 0</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (!=): &apos;a =&gt; &apos;a =&gt; bool;\n</pre><div class="info ">\nNegation of <a href="Pervasives.html#VAL(==)"><code class="code">Pervasives.(==)</code></a>.<br>\n</div>\n<br>\n<h6 id="6_Booleanoperations">Boolean operations</h6><br>\n\n<pre><span class="keyword">let</span> not: bool =&gt; bool;\n</pre><div class="info ">\nThe boolean negation.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&amp;&amp;): bool =&gt; bool =&gt; bool;\n</pre><div class="info ">\nThe boolean &apos;and&apos;. Evaluation is sequential, left-to-right:\n   in <code class="code">e1 &amp;&amp; e2</code>, <code class="code">e1</code> is evaluated first, and if it returns <code class="code">false</code>,\n   <code class="code">e2</code> is not evaluated at all.<br>\n</div>\n\n<pre><span class="keyword">let</span> (&amp;): bool =&gt; bool =&gt; bool;\n</pre><div class="info ">\n<span class="warning">Deprecated.</span><a href="Pervasives.html#VAL(&amp;&amp;)"><code class="code">Pervasives.(&amp;&amp;)</code></a> should be used instead.<br>\n</div>\n\n<pre><span class="keyword">let</span> (||): bool =&gt; bool =&gt; bool;\n</pre><div class="info ">\nThe boolean &apos;or&apos;. Evaluation is sequential, left-to-right:\n   in <code class="code">e1 || e2</code>, <code class="code">e1</code> is evaluated first, and if it returns <code class="code">true</code>,\n   <code class="code">e2</code> is not evaluated at all.<br>\n</div>\n\n<pre><span class="keyword">let</span> (or): bool =&gt; bool =&gt; bool;\n</pre><div class="info ">\n<span class="warning">Deprecated.</span><a href="Pervasives.html#VAL(||)"><code class="code">Pervasives.(||)</code></a> should be used instead.<br>\n</div>\n<br>\n<h6 id="6_Debugging">Debugging</h6><br>\n\n<pre><span class="keyword">let</span> __LOC__: string;\n</pre><div class="info ">\n<code class="code">__LOC__</code> returns the location at which this expression appears in\n    the file currently being parsed by the compiler, with the standard\n    error format of OCaml: &quot;File %S, line %d, characters %d-%d&quot;.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __FILE__: string;\n</pre><div class="info ">\n<code class="code">__FILE__</code> returns the name of the file currently being\n    parsed by the compiler.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __LINE__: int;\n</pre><div class="info ">\n<code class="code">__LINE__</code> returns the line number at which this expression\n    appears in the file currently being parsed by the compiler.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __MODULE__: string;\n</pre><div class="info ">\n<code class="code">__MODULE__</code> returns the module name of the file being\n    parsed by the compiler.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __POS__: (string, int, int, int);\n</pre><div class="info ">\n<code class="code">__POS__</code> returns a tuple <code class="code">(file,lnum,cnum,enum)</code>, corresponding\n    to the location at which this expression appears in the file\n    currently being parsed by the compiler. <code class="code">file</code> is the current\n    filename, <code class="code">lnum</code> the line number, <code class="code">cnum</code> the character position in\n    the line and <code class="code">enum</code> the last character position in the line.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __LOC_OF__: &apos;a =&gt; (string, &apos;a);\n</pre><div class="info ">\n<code class="code">__LOC_OF__ expr</code> returns a pair <code class="code">(loc, expr)</code> where <code class="code">loc</code> is the\n    location of <code class="code">expr</code> in the file currently being parsed by the\n    compiler, with the standard error format of OCaml: &quot;File %S, line\n    %d, characters %d-%d&quot;.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __LINE_OF__: &apos;a =&gt; (int, &apos;a);\n</pre><div class="info ">\n<code class="code">__LINE__ expr</code> returns a pair <code class="code">(line, expr)</code>, where <code class="code">line</code> is the\n    line number at which the expression <code class="code">expr</code> appears in the file\n    currently being parsed by the compiler.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> __POS_OF__: &apos;a =&gt; ((string, int, int, int), &apos;a);\n</pre><div class="info ">\n<code class="code">__POS_OF__ expr</code> returns a pair <code class="code">(loc,expr)</code>, where <code class="code">loc</code> is a\n    tuple <code class="code">(file,lnum,cnum,enum)</code> corresponding to the location at\n    which the expression <code class="code">expr</code> appears in the file currently being\n    parsed by the compiler. <code class="code">file</code> is the current filename, <code class="code">lnum</code> the\n    line number, <code class="code">cnum</code> the character position in the line and <code class="code">enum</code>\n    the last character position in the line.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n<br>\n<h6 id="6_Compositionoperators">Composition operators</h6><br>\n\n<pre><span class="keyword">let</span> (|&gt;): &apos;a =&gt; (&apos;a =&gt; &apos;b) =&gt; &apos;b;\n</pre><div class="info ">\nReverse-application operator: <code class="code">x |&gt; f |&gt; g</code> is exactly equivalent\n to <code class="code">g (f (x))</code>.<br>\n<b>Since</b> 4.01<br>\n</div>\n\n<pre><span class="keyword">let</span> (@@): (&apos;a =&gt; &apos;b) =&gt; &apos;a =&gt; &apos;b;\n</pre><div class="info ">\nApplication operator: <code class="code">g @@ f @@ x</code> is exactly equivalent to\n <code class="code">g (f (x))</code>.<br>\n<b>Since</b> 4.01<br>\n</div>\n<br>\n<h6 id="6_Integerarithmetic">Integer arithmetic</h6><br>\n<br>\n<h6 id="6_Integerarithmetic">Integer arithmetic</h6><br>\n<br>\nIntegers are 31 bits wide (or 63 bits on 64-bit processors).\n   All operations are taken modulo 2<sup class="superscript">31</sup> (or 2<sup class="superscript">63</sup>).\n   They do not fail on overflow.<br>\n\n<pre><span class="keyword">let</span> (~-): int =&gt; int;\n</pre><div class="info ">\nUnary negation. You can also write <code class="code">- e</code> instead of <code class="code">~- e</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (~+): int =&gt; int;\n</pre><div class="info ">\nUnary addition. You can also write <code class="code">+ e</code> instead of <code class="code">~+ e</code>.<br>\n<b>Since</b> 3.12.0<br>\n</div>\n\n<pre><span class="keyword">let</span> succ: int =&gt; int;\n</pre><div class="info ">\n<code class="code">succ x</code> is <code class="code">x + 1</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> pred: int =&gt; int;\n</pre><div class="info ">\n<code class="code">pred x</code> is <code class="code">x - 1</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (+): int =&gt; int =&gt; int;\n</pre><div class="info ">\nInteger addition.<br>\n</div>\n\n<pre><span class="keyword">let</span> (-): int =&gt; int =&gt; int;\n</pre><div class="info ">\nInteger subtraction.<br>\n</div>\n\n<pre><span class="keyword">let</span> ( * ): int =&gt; int =&gt; int;\n</pre><div class="info ">\nInteger multiplication.<br>\n</div>\n\n<pre><span class="keyword">let</span> (/): int =&gt; int =&gt; int;\n</pre><div class="info ">\nInteger division.\n   Raise <code class="code">Division_by_zero</code> if the second argument is 0.\n   Integer division rounds the real quotient of its arguments towards zero.\n   More precisely, if <code class="code">x &gt;= 0</code> and <code class="code">y &gt; 0</code>, <code class="code">x / y</code> is the greatest integer\n   less than or equal to the real quotient of <code class="code">x</code> by <code class="code">y</code>.  Moreover,\n   <code class="code">(- x) / y = x / (- y) = - (x / y)</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (mod): int =&gt; int =&gt; int;\n</pre><div class="info ">\nInteger remainder.  If <code class="code">y</code> is not zero, the result\n   of <code class="code">x mod y</code> satisfies the following properties:\n   <code class="code">x = (x / y) * y + x mod y</code> and\n   <code class="code">abs(x mod y) &lt;= abs(y) - 1</code>.\n   If <code class="code">y = 0</code>, <code class="code">x mod y</code> raises <code class="code">Division_by_zero</code>.\n   Note that <code class="code">x mod y</code> is negative only if <code class="code">x &lt; 0</code>.\n   Raise <code class="code">Division_by_zero</code> if <code class="code">y</code> is zero.<br>\n</div>\n\n<pre><span class="keyword">let</span> abs: int =&gt; int;\n</pre><div class="info ">\nReturn the absolute value of the argument.  Note that this may be\n  negative if the argument is <code class="code">min_int</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> max_int: int;\n</pre><div class="info ">\nThe greatest representable integer.<br>\n</div>\n\n<pre><span class="keyword">let</span> min_int: int;\n</pre><div class="info ">\nThe smallest representable integer.<br>\n</div>\n<br>\n<div class="h7" id="7_Bitwiseoperations">Bitwise operations</div><br>\n\n<pre><span class="keyword">let</span> (land): int =&gt; int =&gt; int;\n</pre><div class="info ">\nBitwise logical and.<br>\n</div>\n\n<pre><span class="keyword">let</span> (lor): int =&gt; int =&gt; int;\n</pre><div class="info ">\nBitwise logical or.<br>\n</div>\n\n<pre><span class="keyword">let</span> (lxor): int =&gt; int =&gt; int;\n</pre><div class="info ">\nBitwise logical exclusive or.<br>\n</div>\n\n<pre><span class="keyword">let</span> lnot: int =&gt; int;\n</pre><div class="info ">\nBitwise logical negation.<br>\n</div>\n\n<pre><span class="keyword">let</span> (lsl): int =&gt; int =&gt; int;\n</pre><div class="info ">\n<code class="code">n lsl m</code> shifts <code class="code">n</code> to the left by <code class="code">m</code> bits.\n   The result is unspecified if <code class="code">m &lt; 0</code> or <code class="code">m &gt;= bitsize</code>,\n   where <code class="code">bitsize</code> is <code class="code">32</code> on a 32-bit platform and\n   <code class="code">64</code> on a 64-bit platform.<br>\n</div>\n\n<pre><span class="keyword">let</span> (lsr): int =&gt; int =&gt; int;\n</pre><div class="info ">\n<code class="code">n lsr m</code> shifts <code class="code">n</code> to the right by <code class="code">m</code> bits.\n   This is a logical shift: zeroes are inserted regardless of\n   the sign of <code class="code">n</code>.\n   The result is unspecified if <code class="code">m &lt; 0</code> or <code class="code">m &gt;= bitsize</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (asr): int =&gt; int =&gt; int;\n</pre><div class="info ">\n<code class="code">n asr m</code> shifts <code class="code">n</code> to the right by <code class="code">m</code> bits.\n   This is an arithmetic shift: the sign bit of <code class="code">n</code> is replicated.\n   The result is unspecified if <code class="code">m &lt; 0</code> or <code class="code">m &gt;= bitsize</code>.<br>\n</div>\n<br>\n<h6 id="6_Floatingpointarithmetic">Floating-point arithmetic</h6>\n<p>\n\n   OCaml&apos;s floating-point numbers follow the\n   IEEE 754 standard, using double precision (64 bits) numbers.\n   Floating-point operations never raise an exception on overflow,\n   underflow, division by zero, etc.  Instead, special IEEE numbers\n   are returned as appropriate, such as <code class="code">infinity</code> for <code class="code">1.0 /. 0.0</code>,\n   <code class="code">neg_infinity</code> for <code class="code">-1.0 /. 0.0</code>, and <code class="code">nan</code> (&apos;not a number&apos;)\n   for <code class="code">0.0 /. 0.0</code>.  These special numbers then propagate through\n   floating-point computations as expected: for instance,\n   <code class="code">1.0 /. infinity</code> is <code class="code">0.0</code>, and any arithmetic operation with <code class="code">nan</code>\n   as argument returns <code class="code">nan</code> as result.<br>\n\n</p><pre><span class="keyword">let</span> (~-.): float =&gt; float;\n</pre><div class="info ">\nUnary negation. You can also write <code class="code">-. e</code> instead of <code class="code">~-. e</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (~+.): float =&gt; float;\n</pre><div class="info ">\nUnary addition. You can also write <code class="code">+. e</code> instead of <code class="code">~+. e</code>.<br>\n<b>Since</b> 3.12.0<br>\n</div>\n\n<pre><span class="keyword">let</span> (+.): float =&gt; float =&gt; float;\n</pre><div class="info ">\nFloating-point addition<br>\n</div>\n\n<pre><span class="keyword">let</span> (-.): float =&gt; float =&gt; float;\n</pre><div class="info ">\nFloating-point subtraction<br>\n</div>\n\n<pre><span class="keyword">let</span> ( *. ): float =&gt; float =&gt; float;\n</pre><div class="info ">\nFloating-point multiplication<br>\n</div>\n\n<pre><span class="keyword">let</span> (/.): float =&gt; float =&gt; float;\n</pre><div class="info ">\nFloating-point division.<br>\n</div>\n\n<pre><span class="keyword">let</span> ( ** ): float =&gt; float =&gt; float;\n</pre><div class="info ">\nExponentiation.<br>\n</div>\n\n<pre><span class="keyword">let</span> sqrt: float =&gt; float;\n</pre><div class="info ">\nSquare root.<br>\n</div>\n\n<pre><span class="keyword">let</span> exp: float =&gt; float;\n</pre><div class="info ">\nExponential.<br>\n</div>\n\n<pre><span class="keyword">let</span> log: float =&gt; float;\n</pre><div class="info ">\nNatural logarithm.<br>\n</div>\n\n<pre><span class="keyword">let</span> log10: float =&gt; float;\n</pre><div class="info ">\nBase 10 logarithm.<br>\n</div>\n\n<pre><span class="keyword">let</span> expm1: float =&gt; float;\n</pre><div class="info ">\n<code class="code">expm1 x</code> computes <code class="code">exp x -. 1.0</code>, giving numerically-accurate results\n    even if <code class="code">x</code> is close to <code class="code">0.0</code>.<br>\n<b>Since</b> 3.12.0<br>\n</div>\n\n<pre><span class="keyword">let</span> log1p: float =&gt; float;\n</pre><div class="info ">\n<code class="code">log1p x</code> computes <code class="code">log(1.0 +. x)</code> (natural logarithm),\n    giving numerically-accurate results even if <code class="code">x</code> is close to <code class="code">0.0</code>.<br>\n<b>Since</b> 3.12.0<br>\n</div>\n\n<pre><span class="keyword">let</span> cos: float =&gt; float;\n</pre><div class="info ">\nCosine.  Argument is in radians.<br>\n</div>\n\n<pre><span class="keyword">let</span> sin: float =&gt; float;\n</pre><div class="info ">\nSine.  Argument is in radians.<br>\n</div>\n\n<pre><span class="keyword">let</span> tan: float =&gt; float;\n</pre><div class="info ">\nTangent.  Argument is in radians.<br>\n</div>\n\n<pre><span class="keyword">let</span> acos: float =&gt; float;\n</pre><div class="info ">\nArc cosine.  The argument must fall within the range <code class="code">[-1.0, 1.0]</code>.\n    Result is in radians and is between <code class="code">0.0</code> and <code class="code">pi</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> asin: float =&gt; float;\n</pre><div class="info ">\nArc sine.  The argument must fall within the range <code class="code">[-1.0, 1.0]</code>.\n    Result is in radians and is between <code class="code">-pi/2</code> and <code class="code">pi/2</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> atan: float =&gt; float;\n</pre><div class="info ">\nArc tangent.\n    Result is in radians and is between <code class="code">-pi/2</code> and <code class="code">pi/2</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> atan2: float =&gt; float =&gt; float;\n</pre><div class="info ">\n<code class="code">atan2 y x</code> returns the arc tangent of <code class="code">y /. x</code>.  The signs of <code class="code">x</code>\n    and <code class="code">y</code> are used to determine the quadrant of the result.\n    Result is in radians and is between <code class="code">-pi</code> and <code class="code">pi</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> hypot: float =&gt; float =&gt; float;\n</pre><div class="info ">\n<code class="code">hypot x y</code> returns <code class="code">sqrt(x *. x + y *. y)</code>, that is, the length\n  of the hypotenuse of a right-angled triangle with sides of length\n  <code class="code">x</code> and <code class="code">y</code>, or, equivalently, the distance of the point <code class="code">(x,y)</code>\n  to origin.<br>\n<b>Since</b> 4.00.0<br>\n</div>\n\n<pre><span class="keyword">let</span> cosh: float =&gt; float;\n</pre><div class="info ">\nHyperbolic cosine.  Argument is in radians.<br>\n</div>\n\n<pre><span class="keyword">let</span> sinh: float =&gt; float;\n</pre><div class="info ">\nHyperbolic sine.  Argument is in radians.<br>\n</div>\n\n<pre><span class="keyword">let</span> tanh: float =&gt; float;\n</pre><div class="info ">\nHyperbolic tangent.  Argument is in radians.<br>\n</div>\n\n<pre><span class="keyword">let</span> ceil: float =&gt; float;\n</pre><div class="info ">\nRound above to an integer value.\n    <code class="code">ceil f</code> returns the least integer value greater than or equal to <code class="code">f</code>.\n    The result is returned as a float.<br>\n</div>\n\n<pre><span class="keyword">let</span> floor: float =&gt; float;\n</pre><div class="info ">\nRound below to an integer value.\n    <code class="code">floor f</code> returns the greatest integer value less than or\n    equal to <code class="code">f</code>.\n    The result is returned as a float.<br>\n</div>\n\n<pre><span class="keyword">let</span> abs_float: float =&gt; float;\n</pre><div class="info ">\n<code class="code">abs_float f</code> returns the absolute value of <code class="code">f</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> copysign: float =&gt; float =&gt; float;\n</pre><div class="info ">\n<code class="code">copysign x y</code> returns a float whose absolute value is that of <code class="code">x</code>\n  and whose sign is that of <code class="code">y</code>.  If <code class="code">x</code> is <code class="code">nan</code>, returns <code class="code">nan</code>.\n  If <code class="code">y</code> is <code class="code">nan</code>, returns either <code class="code">x</code> or <code class="code">-. x</code>, but it is not\n  specified which.<br>\n<b>Since</b> 4.00.0<br>\n</div>\n\n<pre><span class="keyword">let</span> mod_float: float =&gt; float =&gt; float;\n</pre><div class="info ">\n<code class="code">mod_float a b</code> returns the remainder of <code class="code">a</code> with respect to\n   <code class="code">b</code>.  The returned value is <code class="code">a -. n *. b</code>, where <code class="code">n</code>\n   is the quotient <code class="code">a /. b</code> rounded towards zero to an integer.<br>\n</div>\n\n<pre><span class="keyword">let</span> frexp: float =&gt; (float, int);\n</pre><div class="info ">\n<code class="code">frexp f</code> returns the pair of the significant\n   and the exponent of <code class="code">f</code>.  When <code class="code">f</code> is zero, the\n   significant <code class="code">x</code> and the exponent <code class="code">n</code> of <code class="code">f</code> are equal to\n   zero.  When <code class="code">f</code> is non-zero, they are defined by\n   <code class="code">f = x *. 2 ** n</code> and <code class="code">0.5 &lt;= x &lt; 1.0</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> ldexp: float =&gt; int =&gt; float;\n</pre><div class="info ">\n<code class="code">ldexp x n</code> returns <code class="code">x *. 2 ** n</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> modf: float =&gt; (float, float);\n</pre><div class="info ">\n<code class="code">modf f</code> returns the pair of the fractional and integral\n   part of <code class="code">f</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> float: int =&gt; float;\n</pre><div class="info ">\nSame as <a href="Pervasives.html#VALfloat_of_int"><code class="code">Pervasives.float_of_int</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> float_of_int: int =&gt; float;\n</pre><div class="info ">\nConvert an integer to floating-point.<br>\n</div>\n\n<pre><span class="keyword">let</span> truncate: float =&gt; int;\n</pre><div class="info ">\nSame as <a href="Pervasives.html#VALint_of_float"><code class="code">Pervasives.int_of_float</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> int_of_float: float =&gt; int;\n</pre><div class="info ">\nTruncate the given floating-point number to an integer.\n   The result is unspecified if the argument is <code class="code">nan</code> or falls outside the\n   range of representable integers.<br>\n</div>\n\n<pre><span class="keyword">let</span> infinity: float;\n</pre><div class="info ">\nPositive infinity.<br>\n</div>\n\n<pre><span class="keyword">let</span> neg_infinity: float;\n</pre><div class="info ">\nNegative infinity.<br>\n</div>\n\n<pre><span class="keyword">let</span> nan: float;\n</pre><div class="info ">\nA special floating-point value denoting the result of an\n   undefined operation such as <code class="code">0.0 /. 0.0</code>.  Stands for\n   &apos;not a number&apos;.  Any floating-point operation with <code class="code">nan</code> as\n   argument returns <code class="code">nan</code> as result.  As for floating-point comparisons,\n   <code class="code">=</code>, <code class="code">&lt;</code>, <code class="code">&lt;=</code>, <code class="code">&gt;</code> and <code class="code">&gt;=</code> return <code class="code">false</code> and <code class="code">&lt;&gt;</code> returns <code class="code">true</code>\n   if one or both of their arguments is <code class="code">nan</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> max_float: float;\n</pre><div class="info ">\nThe largest positive finite value of type <code class="code">float</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> min_float: float;\n</pre><div class="info ">\nThe smallest positive, non-zero, non-denormalized value of type <code class="code">float</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> epsilon_float: float;\n</pre><div class="info ">\nThe difference between <code class="code">1.0</code> and the smallest exactly representable\n    floating-point number greater than <code class="code">1.0</code>.<br>\n</div>\n\n<pre><span class="keyword">type</span> fpclass = </pre><table class="typetable">\n<tbody><tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>FP_normal</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nNormal number, none of the below<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>FP_subnormal</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nNumber very close to 0.0, has reduced precision<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>FP_zero</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nNumber is 0.0 or -0.0<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>FP_infinite</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nNumber is positive or negative infinity<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>FP_nan</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nNot a number: result of an undefined operation<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr></tbody></table>\n\n<div class="info ">\nThe five classes of floating-point numbers, as determined by\n   the <a href="Pervasives.html#VALclassify_float"><code class="code">Pervasives.classify_float</code></a> function.<br>\n</div>\n\n\n<pre><span class="keyword">let</span> classify_float: float =&gt; fpclass;\n</pre><div class="info ">\nReturn the class of the given floating-point number:\n   normal, subnormal, zero, infinite, or not a number.<br>\n</div>\n<br>\n<h6 id="6_Stringoperations">String operations</h6>\n<p>\n\n   More string operations are provided in module <a href="String.html"><code class="code">String</code></a>.<br>\n\n</p><pre><span class="keyword">let</span> (^): string =&gt; string =&gt; string;\n</pre><div class="info ">\nString concatenation.<br>\n</div>\n<br>\n<h6 id="6_Characteroperations">Character operations</h6>\n<p>\n\n   More character operations are provided in module <a href="Char.html"><code class="code">Char</code></a>.<br>\n\n</p><pre><span class="keyword">let</span> int_of_char: char =&gt; int;\n</pre><div class="info ">\nReturn the ASCII code of the argument.<br>\n</div>\n\n<pre><span class="keyword">let</span> char_of_int: int =&gt; char;\n</pre><div class="info ">\nReturn the character with the given ASCII code.\n   Raise <code class="code">Invalid_argument &quot;char_of_int&quot;</code> if the argument is\n   outside the range 0--255.<br>\n</div>\n<br>\n<h6 id="6_Unitoperations">Unit operations</h6><br>\n\n<pre><span class="keyword">let</span> ignore: &apos;a =&gt; unit;\n</pre><div class="info ">\nDiscard the value of its argument and return <code class="code">()</code>.\n   For instance, <code class="code">ignore(f x)</code> discards the result of\n   the side-effecting function <code class="code">f</code>.  It is equivalent to\n   <code class="code">f x; ()</code>, except that the latter may generate a\n   compiler warning; writing <code class="code">ignore(f x)</code> instead\n   avoids the warning.<br>\n</div>\n<br>\n<h6 id="6_Stringconversionfunctions">String conversion functions</h6><br>\n\n<pre><span class="keyword">let</span> string_of_bool: bool =&gt; string;\n</pre><div class="info ">\nReturn the string representation of a boolean. As the returned values\n   may be shared, the user should not modify them directly.<br>\n</div>\n\n<pre><span class="keyword">let</span> bool_of_string: string =&gt; bool;\n</pre><div class="info ">\nConvert the given string to a boolean.\n   Raise <code class="code">Invalid_argument &quot;bool_of_string&quot;</code> if the string is not\n   <code class="code">&quot;true&quot;</code> or <code class="code">&quot;false&quot;</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> string_of_int: int =&gt; string;\n</pre><div class="info ">\nReturn the string representation of an integer, in decimal.<br>\n</div>\n\n<pre><span class="keyword">let</span> int_of_string: string =&gt; int;\n</pre><div class="info ">\nConvert the given string to an integer.\n   The string is read in decimal (by default) or in hexadecimal (if it\n   begins with <code class="code">0x</code> or <code class="code">0X</code>), octal (if it begins with <code class="code">0o</code> or <code class="code">0O</code>),\n   or binary (if it begins with <code class="code">0b</code> or <code class="code">0B</code>).\n   Raise <code class="code">Failure &quot;int_of_string&quot;</code> if the given string is not\n   a valid representation of an integer, or if the integer represented\n   exceeds the range of integers representable in type <code class="code">int</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> string_of_float: float =&gt; string;\n</pre><div class="info ">\nReturn the string representation of a floating-point number.<br>\n</div>\n\n<pre><span class="keyword">let</span> float_of_string: string =&gt; float;\n</pre><div class="info ">\nConvert the given string to a float.  Raise <code class="code">Failure &quot;float_of_string&quot;</code>\n   if the given string is not a valid representation of a float.<br>\n</div>\n<br>\n<h6 id="6_Pairoperations">Pair operations</h6><br>\n\n<pre><span class="keyword">let</span> fst: (&apos;a, &apos;b) =&gt; &apos;a;\n</pre><div class="info ">\nReturn the first component of a pair.<br>\n</div>\n\n<pre><span class="keyword">let</span> snd: (&apos;a, &apos;b) =&gt; &apos;b;\n</pre><div class="info ">\nReturn the second component of a pair.<br>\n</div>\n<br>\n<h6 id="6_Listoperations">List operations</h6>\n<p>\n\n   More list operations are provided in module <a href="List.html"><code class="code">List</code></a>.<br>\n\n</p><pre><span class="keyword">let</span> (@): list &apos;a =&gt; list &apos;a =&gt; list &apos;a;\n</pre><div class="info ">\nList concatenation.<br>\n</div>\n<br>\n<h6 id="6_Inputoutput">Input/output</h6>\n    Note: all input/output functions can raise <code class="code">Sys_error</code> when the system\n    calls they invoke fail.<br>\n\n<pre><span class="keyword">type</span> in_channel;\n</pre>\n<div class="info ">\nThe type of input channel.<br>\n</div>\n\n\n<pre><span class="keyword">type</span> out_channel;\n</pre>\n<div class="info ">\nThe type of output channel.<br>\n</div>\n\n\n<pre><span class="keyword">let</span> stdin: in_channel;\n</pre><div class="info ">\nThe standard input for the process.<br>\n</div>\n\n<pre><span class="keyword">let</span> stdout: out_channel;\n</pre><div class="info ">\nThe standard output for the process.<br>\n</div>\n\n<pre><span class="keyword">let</span> stderr: out_channel;\n</pre><div class="info ">\nThe standard error output for the process.<br>\n</div>\n<br>\n<div class="h7" id="7_Outputfunctionsonstandardoutput">Output functions on standard output</div><br>\n\n<pre><span class="keyword">let</span> print_char: char =&gt; unit;\n</pre><div class="info ">\nPrint a character on standard output.<br>\n</div>\n\n<pre><span class="keyword">let</span> print_string: string =&gt; unit;\n</pre><div class="info ">\nPrint a string on standard output.<br>\n</div>\n\n<pre><span class="keyword">let</span> print_bytes: bytes =&gt; unit;\n</pre><div class="info ">\nPrint a byte sequence on standard output.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> print_int: int =&gt; unit;\n</pre><div class="info ">\nPrint an integer, in decimal, on standard output.<br>\n</div>\n\n<pre><span class="keyword">let</span> print_float: float =&gt; unit;\n</pre><div class="info ">\nPrint a floating-point number, in decimal, on standard output.<br>\n</div>\n\n<pre><span class="keyword">let</span> print_endline: string =&gt; unit;\n</pre><div class="info ">\nPrint a string, followed by a newline character, on\n   standard output and flush standard output.<br>\n</div>\n\n<pre><span class="keyword">let</span> print_newline: unit =&gt; unit;\n</pre><div class="info ">\nPrint a newline character on standard output, and flush\n   standard output. This can be used to simulate line\n   buffering of standard output.<br>\n</div>\n<br>\n<div class="h7" id="7_Outputfunctionsonstandarderror">Output functions on standard error</div><br>\n\n<pre><span class="keyword">let</span> prerr_char: char =&gt; unit;\n</pre><div class="info ">\nPrint a character on standard error.<br>\n</div>\n\n<pre><span class="keyword">let</span> prerr_string: string =&gt; unit;\n</pre><div class="info ">\nPrint a string on standard error.<br>\n</div>\n\n<pre><span class="keyword">let</span> prerr_bytes: bytes =&gt; unit;\n</pre><div class="info ">\nPrint a byte sequence on standard error.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> prerr_int: int =&gt; unit;\n</pre><div class="info ">\nPrint an integer, in decimal, on standard error.<br>\n</div>\n\n<pre><span class="keyword">let</span> prerr_float: float =&gt; unit;\n</pre><div class="info ">\nPrint a floating-point number, in decimal, on standard error.<br>\n</div>\n\n<pre><span class="keyword">let</span> prerr_endline: string =&gt; unit;\n</pre><div class="info ">\nPrint a string, followed by a newline character on standard\n   error and flush standard error.<br>\n</div>\n\n<pre><span class="keyword">let</span> prerr_newline: unit =&gt; unit;\n</pre><div class="info ">\nPrint a newline character on standard error, and flush\n   standard error.<br>\n</div>\n<br>\n<div class="h7" id="7_Inputfunctionsonstandardinput">Input functions on standard input</div><br>\n\n<pre><span class="keyword">let</span> read_line: unit =&gt; string;\n</pre><div class="info ">\nFlush standard output, then read characters from standard input\n   until a newline character is encountered. Return the string of\n   all characters read, without the newline character at the end.<br>\n</div>\n\n<pre><span class="keyword">let</span> read_int: unit =&gt; int;\n</pre><div class="info ">\nFlush standard output, then read one line from standard input\n   and convert it to an integer. Raise <code class="code">Failure &quot;int_of_string&quot;</code>\n   if the line read is not a valid representation of an integer.<br>\n</div>\n\n<pre><span class="keyword">let</span> read_float: unit =&gt; float;\n</pre><div class="info ">\nFlush standard output, then read one line from standard input\n   and convert it to a floating-point number.\n   The result is unspecified if the line read is not a valid\n   representation of a floating-point number.<br>\n</div>\n<br>\n<div class="h7" id="7_Generaloutputfunctions">General output functions</div><br>\n\n<pre><span class="keyword">type</span> open_flag = </pre><table class="typetable">\n<tbody><tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_rdonly</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nopen for reading.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_wronly</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nopen for writing.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_append</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nopen for appending: always write at end of file.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_creat</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\ncreate the file if it does not exist.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_trunc</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nempty the file if it already exists.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_excl</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nfail if Open_creat and the file already exists.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_binary</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nopen in binary mode (no conversion).<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_text</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nopen in text mode (may perform conversions).<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr>\n<tr>\n<td align="left" valign="top">\n<code>|</code></td>\n<td align="left" valign="top">\n<code>Open_nonblock</code></td>\n<td class="typefieldcomment" align="left" valign="top"><code>(*</code></td><td class="typefieldcomment" align="left" valign="top"><div class="info ">\nopen in non-blocking mode.<br>\n</div>\n</td><td class="typefieldcomment" align="left" valign="bottom"><code>*)</code></td>\n</tr></tbody></table>\n\n<div class="info ">\nOpening modes for <a href="Pervasives.html#VALopen_out_gen"><code class="code">Pervasives.open_out_gen</code></a> and\n  <a href="Pervasives.html#VALopen_in_gen"><code class="code">Pervasives.open_in_gen</code></a>.<br>\n</div>\n\n\n<pre><span class="keyword">let</span> open_out: string =&gt; out_channel;\n</pre><div class="info ">\nOpen the named file for writing, and return a new output channel\n   on that file, positionned at the beginning of the file. The\n   file is truncated to zero length if it already exists. It\n   is created if it does not already exists.<br>\n</div>\n\n<pre><span class="keyword">let</span> open_out_bin: string =&gt; out_channel;\n</pre><div class="info ">\nSame as <a href="Pervasives.html#VALopen_out"><code class="code">Pervasives.open_out</code></a>, but the file is opened in binary mode,\n   so that no translation takes place during writes. On operating\n   systems that do not distinguish between text mode and binary\n   mode, this function behaves like <a href="Pervasives.html#VALopen_out"><code class="code">Pervasives.open_out</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> open_out_gen: list open_flag =&gt; int =&gt; string =&gt; out_channel;\n</pre><div class="info ">\n<code class="code">open_out_gen mode perm filename</code> opens the named file for writing,\n   as described above. The extra argument <code class="code">mode</code>\n   specify the opening mode. The extra argument <code class="code">perm</code> specifies\n   the file permissions, in case the file must be created.\n   <a href="Pervasives.html#VALopen_out"><code class="code">Pervasives.open_out</code></a> and <a href="Pervasives.html#VALopen_out_bin"><code class="code">Pervasives.open_out_bin</code></a> are special\n   cases of this function.<br>\n</div>\n\n<pre><span class="keyword">let</span> flush: out_channel =&gt; unit;\n</pre><div class="info ">\nFlush the buffer associated with the given output channel,\n   performing all pending writes on that channel.\n   Interactive programs must be careful about flushing standard\n   output and standard error at the right time.<br>\n</div>\n\n<pre><span class="keyword">let</span> flush_all: unit =&gt; unit;\n</pre><div class="info ">\nFlush all open output channels; ignore errors.<br>\n</div>\n\n<pre><span class="keyword">let</span> output_char: out_channel =&gt; char =&gt; unit;\n</pre><div class="info ">\nWrite the character on the given output channel.<br>\n</div>\n\n<pre><span class="keyword">let</span> output_string: out_channel =&gt; string =&gt; unit;\n</pre><div class="info ">\nWrite the string on the given output channel.<br>\n</div>\n\n<pre><span class="keyword">let</span> output_bytes: out_channel =&gt; bytes =&gt; unit;\n</pre><div class="info ">\nWrite the byte sequence on the given output channel.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> output: out_channel =&gt; bytes =&gt; int =&gt; int =&gt; unit;\n</pre><div class="info ">\n<code class="code">output oc buf pos len</code> writes <code class="code">len</code> characters from byte sequence <code class="code">buf</code>,\n   starting at offset <code class="code">pos</code>, to the given output channel <code class="code">oc</code>.\n   Raise <code class="code">Invalid_argument &quot;output&quot;</code> if <code class="code">pos</code> and <code class="code">len</code> do not\n   designate a valid range of <code class="code">buf</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> output_substring: out_channel =&gt; string =&gt; int =&gt; int =&gt; unit;\n</pre><div class="info ">\nSame as <code class="code">output</code> but take a string as argument instead of\n   a byte sequence.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> output_byte: out_channel =&gt; int =&gt; unit;\n</pre><div class="info ">\nWrite one 8-bit integer (as the single character with that code)\n   on the given output channel. The given integer is taken modulo\n   256.<br>\n</div>\n\n<pre><span class="keyword">let</span> output_binary_int: out_channel =&gt; int =&gt; unit;\n</pre><div class="info ">\nWrite one integer in binary format (4 bytes, big-endian)\n   on the given output channel.\n   The given integer is taken modulo 2<sup class="superscript">32</sup>.\n   The only reliable way to read it back is through the\n   <a href="Pervasives.html#VALinput_binary_int"><code class="code">Pervasives.input_binary_int</code></a> function. The format is compatible across\n   all machines for a given version of OCaml.<br>\n</div>\n\n<pre><span class="keyword">let</span> output_value: out_channel =&gt; &apos;a =&gt; unit;\n</pre><div class="info ">\nWrite the representation of a structured value of any type\n   to a channel. Circularities and sharing inside the value\n   are detected and preserved. The object can be read back,\n   by the function <a href="Pervasives.html#VALinput_value"><code class="code">Pervasives.input_value</code></a>. See the description of module\n   <a href="Marshal.html"><code class="code">Marshal</code></a> for more information. <a href="Pervasives.html#VALoutput_value"><code class="code">Pervasives.output_value</code></a> is equivalent\n   to <a href="Marshal.html#VALto_channel"><code class="code">Marshal.to_channel</code></a> with an empty list of flags.<br>\n</div>\n\n<pre><span class="keyword">let</span> seek_out: out_channel =&gt; int =&gt; unit;\n</pre><div class="info ">\n<code class="code">seek_out chan pos</code> sets the current writing position to <code class="code">pos</code>\n   for channel <code class="code">chan</code>. This works only for regular files. On\n   files of other kinds (such as terminals, pipes and sockets),\n   the behavior is unspecified.<br>\n</div>\n\n<pre><span class="keyword">let</span> pos_out: out_channel =&gt; int;\n</pre><div class="info ">\nReturn the current writing position for the given channel.  Does\n    not work on channels opened with the <code class="code">Open_append</code> flag (returns\n    unspecified results).<br>\n</div>\n\n<pre><span class="keyword">let</span> out_channel_length: out_channel =&gt; int;\n</pre><div class="info ">\nReturn the size (number of characters) of the regular file\n   on which the given channel is opened.  If the channel is opened\n    on a file that is not a regular file, the result is meaningless.<br>\n</div>\n\n<pre><span class="keyword">let</span> close_out: out_channel =&gt; unit;\n</pre><div class="info ">\nClose the given channel, flushing all buffered write operations.\n   Output functions raise a <code class="code">Sys_error</code> exception when they are\n   applied to a closed output channel, except <code class="code">close_out</code> and <code class="code">flush</code>,\n   which do nothing when applied to an already closed channel.\n   Note that <code class="code">close_out</code> may raise <code class="code">Sys_error</code> if the operating\n   system signals an error when flushing or closing.<br>\n</div>\n\n<pre><span class="keyword">let</span> close_out_noerr: out_channel =&gt; unit;\n</pre><div class="info ">\nSame as <code class="code">close_out</code>, but ignore all errors.<br>\n</div>\n\n<pre><span class="keyword">let</span> set_binary_mode_out: out_channel =&gt; bool =&gt; unit;\n</pre><div class="info ">\n<code class="code">set_binary_mode_out oc true</code> sets the channel <code class="code">oc</code> to binary\n   mode: no translations take place during output.\n   <code class="code">set_binary_mode_out oc false</code> sets the channel <code class="code">oc</code> to text\n   mode: depending on the operating system, some translations\n   may take place during output.  For instance, under Windows,\n   end-of-lines will be translated from <code class="code">\\n</code> to <code class="code">\\r\\n</code>.\n   This function has no effect under operating systems that\n   do not distinguish between text mode and binary mode.<br>\n</div>\n<br>\n<div class="h7" id="7_Generalinputfunctions">General input functions</div><br>\n\n<pre><span class="keyword">let</span> open_in: string =&gt; in_channel;\n</pre><div class="info ">\nOpen the named file for reading, and return a new input channel\n   on that file, positionned at the beginning of the file.<br>\n</div>\n\n<pre><span class="keyword">let</span> open_in_bin: string =&gt; in_channel;\n</pre><div class="info ">\nSame as <a href="Pervasives.html#VALopen_in"><code class="code">Pervasives.open_in</code></a>, but the file is opened in binary mode,\n   so that no translation takes place during reads. On operating\n   systems that do not distinguish between text mode and binary\n   mode, this function behaves like <a href="Pervasives.html#VALopen_in"><code class="code">Pervasives.open_in</code></a>.<br>\n</div>\n\n<pre><span class="keyword">let</span> open_in_gen: list open_flag =&gt; int =&gt; string =&gt; in_channel;\n</pre><div class="info ">\n<code class="code">open_in_gen mode perm filename</code> opens the named file for reading,\n   as described above. The extra arguments\n   <code class="code">mode</code> and <code class="code">perm</code> specify the opening mode and file permissions.\n   <a href="Pervasives.html#VALopen_in"><code class="code">Pervasives.open_in</code></a> and <a href="Pervasives.html#VALopen_in_bin"><code class="code">Pervasives.open_in_bin</code></a> are special\n   cases of this function.<br>\n</div>\n\n<pre><span class="keyword">let</span> input_char: in_channel =&gt; char;\n</pre><div class="info ">\nRead one character from the given input channel.\n   Raise <code class="code">End_of_file</code> if there are no more characters to read.<br>\n</div>\n\n<pre><span class="keyword">let</span> input_line: in_channel =&gt; string;\n</pre><div class="info ">\nRead characters from the given input channel, until a\n   newline character is encountered. Return the string of\n   all characters read, without the newline character at the end.\n   Raise <code class="code">End_of_file</code> if the end of the file is reached\n   at the beginning of line.<br>\n</div>\n\n<pre><span class="keyword">let</span> input: in_channel =&gt; bytes =&gt; int =&gt; int =&gt; int;\n</pre><div class="info ">\n<code class="code">input ic buf pos len</code> reads up to <code class="code">len</code> characters from\n   the given channel <code class="code">ic</code>, storing them in byte sequence <code class="code">buf</code>, starting at\n   character number <code class="code">pos</code>.\n   It returns the actual number of characters read, between 0 and\n   <code class="code">len</code> (inclusive).\n   A return value of 0 means that the end of file was reached.\n   A return value between 0 and <code class="code">len</code> exclusive means that\n   not all requested <code class="code">len</code> characters were read, either because\n   no more characters were available at that time, or because\n   the implementation found it convenient to do a partial read;\n   <code class="code">input</code> must be called again to read the remaining characters,\n   if desired.  (See also <a href="Pervasives.html#VALreally_input"><code class="code">Pervasives.really_input</code></a> for reading\n   exactly <code class="code">len</code> characters.)\n   Exception <code class="code">Invalid_argument &quot;input&quot;</code> is raised if <code class="code">pos</code> and <code class="code">len</code>\n   do not designate a valid range of <code class="code">buf</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> really_input: in_channel =&gt; bytes =&gt; int =&gt; int =&gt; unit;\n</pre><div class="info ">\n<code class="code">really_input ic buf pos len</code> reads <code class="code">len</code> characters from channel <code class="code">ic</code>,\n   storing them in byte sequence <code class="code">buf</code>, starting at character number <code class="code">pos</code>.\n   Raise <code class="code">End_of_file</code> if the end of file is reached before <code class="code">len</code>\n   characters have been read.\n   Raise <code class="code">Invalid_argument &quot;really_input&quot;</code> if\n   <code class="code">pos</code> and <code class="code">len</code> do not designate a valid range of <code class="code">buf</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> really_input_string: in_channel =&gt; int =&gt; string;\n</pre><div class="info ">\n<code class="code">really_input_string ic len</code> reads <code class="code">len</code> characters from channel <code class="code">ic</code>\n   and returns them in a new string.\n   Raise <code class="code">End_of_file</code> if the end of file is reached before <code class="code">len</code>\n   characters have been read.<br>\n<b>Since</b> 4.02.0<br>\n</div>\n\n<pre><span class="keyword">let</span> input_byte: in_channel =&gt; int;\n</pre><div class="info ">\nSame as <a href="Pervasives.html#VALinput_char"><code class="code">Pervasives.input_char</code></a>, but return the 8-bit integer representing\n   the character.\n   Raise <code class="code">End_of_file</code> if an end of file was reached.<br>\n</div>\n\n<pre><span class="keyword">let</span> input_binary_int: in_channel =&gt; int;\n</pre><div class="info ">\nRead an integer encoded in binary format (4 bytes, big-endian)\n   from the given input channel. See <a href="Pervasives.html#VALoutput_binary_int"><code class="code">Pervasives.output_binary_int</code></a>.\n   Raise <code class="code">End_of_file</code> if an end of file was reached while reading the\n   integer.<br>\n</div>\n\n<pre><span class="keyword">let</span> input_value: in_channel =&gt; &apos;a;\n</pre><div class="info ">\nRead the representation of a structured value, as produced\n   by <a href="Pervasives.html#VALoutput_value"><code class="code">Pervasives.output_value</code></a>, and return the corresponding value.\n   This function is identical to <a href="Marshal.html#VALfrom_channel"><code class="code">Marshal.from_channel</code></a>;\n   see the description of module <a href="Marshal.html"><code class="code">Marshal</code></a> for more information,\n   in particular concerning the lack of type safety.<br>\n</div>\n\n<pre><span class="keyword">let</span> seek_in: in_channel =&gt; int =&gt; unit;\n</pre><div class="info ">\n<code class="code">seek_in chan pos</code> sets the current reading position to <code class="code">pos</code>\n   for channel <code class="code">chan</code>. This works only for regular files. On\n   files of other kinds, the behavior is unspecified.<br>\n</div>\n\n<pre><span class="keyword">let</span> pos_in: in_channel =&gt; int;\n</pre><div class="info ">\nReturn the current reading position for the given channel.<br>\n</div>\n\n<pre><span class="keyword">let</span> in_channel_length: in_channel =&gt; int;\n</pre><div class="info ">\nReturn the size (number of characters) of the regular file\n    on which the given channel is opened.  If the channel is opened\n    on a file that is not a regular file, the result is meaningless.\n    The returned size does not take into account the end-of-line\n    translations that can be performed when reading from a channel\n    opened in text mode.<br>\n</div>\n\n<pre><span class="keyword">let</span> close_in: in_channel =&gt; unit;\n</pre><div class="info ">\nClose the given channel.  Input functions raise a <code class="code">Sys_error</code>\n  exception when they are applied to a closed input channel,\n  except <code class="code">close_in</code>, which does nothing when applied to an already\n  closed channel.<br>\n</div>\n\n<pre><span class="keyword">let</span> close_in_noerr: in_channel =&gt; unit;\n</pre><div class="info ">\nSame as <code class="code">close_in</code>, but ignore all errors.<br>\n</div>\n\n<pre><span class="keyword">let</span> set_binary_mode_in: in_channel =&gt; bool =&gt; unit;\n</pre><div class="info ">\n<code class="code">set_binary_mode_in ic true</code> sets the channel <code class="code">ic</code> to binary\n   mode: no translations take place during input.\n   <code class="code">set_binary_mode_out ic false</code> sets the channel <code class="code">ic</code> to text\n   mode: depending on the operating system, some translations\n   may take place during input.  For instance, under Windows,\n   end-of-lines will be translated from <code class="code">\\r\\n</code> to <code class="code">\\n</code>.\n   This function has no effect under operating systems that\n   do not distinguish between text mode and binary mode.<br>\n</div>\n<br>\n<div class="h7" id="7_Operationsonlargefiles">Operations on large files</div><br>\n\n<pre><span class="keyword">module</span> LargeFile: sig .. end</pre><div class="info">\nOperations on large files.\n</div>\n<br>\n<h6 id="6_References">References</h6><br>\n\n<pre><span class="keyword">type</span> &apos;a ref = {</pre><table class="typetable">\n<tbody><tr>\n<td align="left" valign="top">\n<code>&#xA0;&#xA0;</code></td>\n<td align="left" valign="top">\n<code>mutable&#xA0;contents&#xA0;: &apos;a;</code></td>\n\n</tr></tbody></table>\n}\n\n<div class="info ">\nThe type of references (mutable indirection cells) containing\n   a value of type <code class="code">&apos;a</code>.<br>\n</div>\n\n\n<pre><span class="keyword">let</span> ref: &apos;a =&gt; ref &apos;a;\n</pre><div class="info ">\nReturn a fresh reference containing the given value.<br>\n</div>\n\n<pre><span class="keyword">let</span> (!): ref &apos;a =&gt; &apos;a;\n</pre><div class="info ">\n<code class="code">!r</code> returns the current contents of reference <code class="code">r</code>.\n   Equivalent to <code class="code">fun r -&gt; r.contents</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> (:=): ref &apos;a =&gt; &apos;a =&gt; unit;\n</pre><div class="info ">\n<code class="code">r := a</code> stores the value of <code class="code">a</code> in reference <code class="code">r</code>.\n   Equivalent to <code class="code">fun r v -&gt; r.contents &lt;- v</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> incr: ref int =&gt; unit;\n</pre><div class="info ">\nIncrement the integer contained in the given reference.\n   Equivalent to <code class="code">fun r -&gt; r := succ !r</code>.<br>\n</div>\n\n<pre><span class="keyword">let</span> decr: ref int =&gt; unit;\n</pre><div class="info ">\nDecrement the integer contained in the given reference.\n   Equivalent to <code class="code">fun r -&gt; r := pred !r</code>.<br>\n</div>\n<br>\n<h6 id="6_Operationsonformatstrings">Operations on format strings</h6><br>\n<br>\n<h6 id="6_Operationsonformatstrings">Operations on format strings</h6><br>\n<br>\nFormat strings are character strings with special lexical conventions\n  that defines the functionality of formatted input/output functions. Format\n  strings are used to read data with formatted input functions from module\n  <a href="Scanf.html"><code class="code">Scanf</code></a> and to print data with formatted output functions from modules\n  <a href="Printf.html"><code class="code">Printf</code></a> and <a href="Format.html"><code class="code">Format</code></a>.\n<p>\n\n  Format strings are made of three kinds of entities:</p><ul>\n<li><em>conversions specifications</em>, introduced by the special character <code class="code">&apos;%&apos;</code>\n    followed by one or more characters specifying what kind of argument to\n    read or print,</li>\n<li><em>formatting indications</em>, introduced by the special character <code class="code">&apos;@&apos;</code>\n    followed by one or more characters specifying how to read or print the\n    argument,</li>\n<li><em>plain characters</em> that are regular characters with usual lexical\n    conventions. Plain characters specify string literals to be read in the\n    input or printed in the output.</li>\n</ul>\n\n  There is an additional lexical rule to escape the special characters <code class="code">&apos;%&apos;</code>\n  and <code class="code">&apos;@&apos;</code> in format strings: if a special character follows a <code class="code">&apos;%&apos;</code>\n  character, it is treated as a plain character. In other words, <code class="code">&quot;%%&quot;</code> is\n  considered as a plain <code class="code">&apos;%&apos;</code> and <code class="code">&quot;%@&quot;</code> as a plain <code class="code">&apos;@&apos;</code>.\n<p>\n\n  For more information about conversion specifications and formatting\n  indications available, read the documentation of modules <a href="Scanf.html"><code class="code">Scanf</code></a>,\n  <a href="Printf.html"><code class="code">Printf</code></a> and <a href="Format.html"><code class="code">Format</code></a>.<br>\n<br>\n</p><h6 id="6_Operationsonformatstrings">Operations on format strings</h6><br>\n<br>\nFormat strings are character strings with special lexical conventions\n  that defines the functionality of formatted input/output functions. Format\n  strings are used to read data with formatted input functions from module\n  <a href="Scanf.html"><code class="code">Scanf</code></a> and to print data with formatted output functions from modules\n  <a href="Printf.html"><code class="code">Printf</code></a> and <a href="Format.html"><code class="code">Format</code></a>.\n<p>\n\n  Format strings are made of three kinds of entities:</p><ul>\n<li><em>conversions specifications</em>, introduced by the special character <code class="code">&apos;%&apos;</code>\n    followed by one or more characters specifying what kind of argument to\n    read or print,</li>\n<li><em>formatting indications</em>, introduced by the special character <code class="code">&apos;@&apos;</code>\n    followed by one or more characters specifying how to read or print the\n    argument,</li>\n<li><em>plain characters</em> that are regular characters with usual lexical\n    conventions. Plain characters specify string literals to be read in the\n    input or printed in the output.</li>\n</ul>\n\n  There is an additional lexical rule to escape the special characters <code class="code">&apos;%&apos;</code>\n  and <code class="code">&apos;@&apos;</code> in format strings: if a special character follows a <code class="code">&apos;%&apos;</code>\n  character, it is treated as a plain character. In other words, <code class="code">&quot;%%&quot;</code> is\n  considered as a plain <code class="code">&apos;%&apos;</code> and <code class="code">&quot;%@&quot;</code> as a plain <code class="code">&apos;@&apos;</code>.\n<p>\n\n  For more information about conversion specifications and formatting\n  indications available, read the documentation of modules <a href="Scanf.html"><code class="code">Scanf</code></a>,\n  <a href="Printf.html"><code class="code">Printf</code></a> and <a href="Format.html"><code class="code">Format</code></a>.<br>\n<br>\nFormat strings have a general and highly polymorphic type\n    <code class="code">(&apos;a, &apos;b, &apos;c, &apos;d, &apos;e, &apos;f) format6</code>.\n    The two simplified types, <code class="code">format</code> and <code class="code">format4</code> below are\n    included for backward compatibility with earlier releases of\n    OCaml.\n</p><p>\n\n    The meaning of format string type parameters is as follows:\n</p><p>\n</p><ul>\n<li><code class="code">&apos;a</code> is the type of the parameters of the format for formatted output\n      functions (<code class="code">printf</code>-style functions);\n      <code class="code">&apos;a</code> is the type of the values read by the format for formatted input\n      functions (<code class="code">scanf</code>-style functions).</li>\n</ul>\n<ul>\n<li><code class="code">&apos;b</code> is the type of input source for formatted input functions and the\n      type of output target for formatted output functions.\n      For <code class="code">printf</code>-style functions from module <code class="code">Printf</code>, <code class="code">&apos;b</code> is typically\n      <code class="code">out_channel</code>;\n      for <code class="code">printf</code>-style functions from module <code class="code">Format</code>, <code class="code">&apos;b</code> is typically\n      <code class="code">Format.formatter</code>;\n      for <code class="code">scanf</code>-style functions from module <code class="code">Scanf</code>, <code class="code">&apos;b</code> is typically\n      <code class="code">Scanf.Scanning.in_channel</code>.</li>\n</ul>\n\n      Type argument <code class="code">&apos;b</code> is also the type of the first argument given to\n      user&apos;s defined printing functions for <code class="code">%a</code> and <code class="code">%t</code> conversions,\n      and user&apos;s defined reading functions for <code class="code">%r</code> conversion.\n<p>\n</p><ul>\n<li><code class="code">&apos;c</code> is the type of the result of the <code class="code">%a</code> and <code class="code">%t</code> printing\n      functions, and also the type of the argument transmitted to the\n      first argument of <code class="code">kprintf</code>-style functions or to the\n      <code class="code">kscanf</code>-style functions.</li>\n</ul>\n<ul>\n<li><code class="code">&apos;d</code> is the type of parameters for the <code class="code">scanf</code>-style functions.</li>\n</ul>\n<ul>\n<li><code class="code">&apos;e</code> is the type of the receiver function for the <code class="code">scanf</code>-style functions.</li>\n</ul>\n<ul>\n<li><code class="code">&apos;f</code> is the final result type of a formatted input/output function\n      invocation: for the <code class="code">printf</code>-style functions, it is typically <code class="code">unit</code>;\n      for the <code class="code">scanf</code>-style functions, it is typically the result type of the\n      receiver function.</li>\n</ul>\n<br>\n\n<pre><span class="keyword">type</span> format6 &apos;a &apos;b &apos;c &apos;d &apos;e &apos;f = CamlinternalFormatBasics.format6 &apos;a &apos;b &apos;c &apos;d &apos;e &apos;f;\n</pre>\n\n\n<pre><span class="keyword">type</span> format4 &apos;a &apos;b &apos;c &apos;d = format6 &apos;a &apos;b &apos;c &apos;c &apos;c &apos;d;\n</pre>\n\n\n<pre><span class="keyword">type</span> format &apos;a &apos;b &apos;c = format4 &apos;a &apos;b &apos;c &apos;c;\n</pre>\n\n\n<pre><span class="keyword">let</span> string_of_format: format6 &apos;a &apos;b &apos;c &apos;d &apos;e &apos;f =&gt; string;\n</pre><div class="info ">\nConverts a format string into a string.<br>\n</div>\n\n<pre><span class="keyword">let</span> format_of_string: format6 &apos;a &apos;b &apos;c &apos;d &apos;e &apos;f =&gt; format6 &apos;a &apos;b &apos;c &apos;d &apos;e &apos;f;\n</pre><div class="info ">\n<code class="code">format_of_string s</code> returns a format string read from the string\n    literal <code class="code">s</code>.\n    Note: <code class="code">format_of_string</code> can not convert a string argument that is not a\n    literal. If you need this functionality, use the more general\n    <a href="Scanf.html#VALformat_from_string"><code class="code">Scanf.format_from_string</code></a> function.<br>\n</div>\n\n<pre><span class="keyword">let</span> (^^): format6 &apos;a &apos;b &apos;c &apos;d &apos;e &apos;f =&gt; format6 &apos;f &apos;b &apos;c &apos;e &apos;g &apos;h =&gt; format6 &apos;a &apos;b &apos;c &apos;d &apos;g &apos;h;\n</pre><div class="info ">\n<code class="code">f1 ^^ f2</code> catenates format strings <code class="code">f1</code> and <code class="code">f2</code>. The result is a\n  format string that behaves as the concatenation of format strings <code class="code">f1</code> and\n  <code class="code">f2</code>: in case of formatted output, it accepts arguments from <code class="code">f1</code>, then\n  arguments from <code class="code">f2</code>; in case of formatted input, it returns results from\n  <code class="code">f1</code>, then results from <code class="code">f2</code>.<br>\n</div>\n<br>\n<h6 id="6_Programtermination">Program termination</h6><br>\n\n<pre><span class="keyword">let</span> exit: int =&gt; &apos;a;\n</pre><div class="info ">\nTerminate the process, returning the given status code\n   to the operating system: usually 0 to indicate no errors,\n   and a small positive integer to indicate failure.\n   All open output channels are flushed with <code class="code">flush_all</code>.\n   An implicit <code class="code">exit 0</code> is performed each time a program\n   terminates normally.  An implicit <code class="code">exit 2</code> is performed if the program\n   terminates early because of an uncaught exception.<br>\n</div>\n\n<pre><span class="keyword">let</span> at_exit: (unit =&gt; unit) =&gt; unit;\n</pre><div class="info ">\nRegister the given function to be called at program\n   termination time. The functions registered with <code class="code">at_exit</code>\n   will be called when the program executes <a href="Pervasives.html#VALexit"><code class="code">Pervasives.exit</code></a>,\n   or terminates, either normally or because of an uncaught exception.\n   The functions are called in &apos;last in, first out&apos; order:\n   the function most recently added with <code class="code">at_exit</code> is called first.<br>\n</div>\n</div>'
}}},pathContext:{relativePath:"api/Pervasives.html"}}}});
//# sourceMappingURL=path---api-pervasives-html-1300e25097c4589b541e.js.map